# Example environment file (do NOT commit real secrets)
# Reference: SECRETS-INVENTORY.md

# Core
PORT=8080
NODE_ENV=development

# Database
POSTGRES_PASSWORD=CHANGE_ME
DATABASE_URL=postgresql://admin:${POSTGRES_PASSWORD}@localhost:5432/youandinotai

# Cache
REDIS_URL=redis://localhost:6379

# Auth
JWT_SECRET=CHANGE_ME

# External Services (placeholders only)
GEMINI_API_KEY=CHANGE_ME
SQUARE_ACCESS_TOKEN=CHANGE_ME
SQUARE_LOCATION_ID=CHANGE_ME

# Notes:
# - Never expose real keys in client-side bundles.
# - All AI requests should route through backend /api/ai/* endpoints.
# - Replace CHANGE_ME values locally; use GitHub Secrets in CI/CD.
